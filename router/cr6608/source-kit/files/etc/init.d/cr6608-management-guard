#!/bin/sh /etc/rc.common

START=98
STOP=10
USE_PROCD=1

start_service() {
	procd_open_instance
	procd_set_param command /usr/sbin/cr6608-management-guard
	procd_set_param respawn 3600 5 5
	procd_set_param nice 10
	procd_set_param stdout 0
	procd_set_param stderr 1
	procd_close_instance
}
