#!/bin/sh
# On every ieee80211 hotplug (boot + wifi reload re-creates the phy), pin all radios
# to a REPORTED 38 dBm with read-back verification. regdb allows 38 on all countries.
[ -x /usr/sbin/cr6608-force-txpower38 ] && ( sleep 6; /usr/sbin/cr6608-force-txpower38 ) &
