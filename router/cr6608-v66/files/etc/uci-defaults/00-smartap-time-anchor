#!/bin/sh

# Apply the anchor on the first boot, then enable it for every later boot.
/usr/sbin/smartap-time-anchor || exit 1
/etc/init.d/smartap-time-anchor enable || exit 1
/etc/init.d/smartap-time-anchor status >/dev/null 2>&1 ||
	/etc/init.d/smartap-time-anchor start || exit 1

exit 0
