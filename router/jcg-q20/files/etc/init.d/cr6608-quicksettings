#!/bin/sh /etc/rc.common

START=99

start() {
	return 0
}

reload_service() {
	# cr6608quick is saved metadata, not a daemon configuration. Reapplying it
	# here would overwrite newer live wireless values with an older profile.
	return 0
}

restart() {
	reload_service
}
