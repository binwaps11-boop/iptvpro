# مصفوفة تتبع متطلبات مدير الكروت

كل متطلب صرّح به المستخدم، وأين نُفذ، وحالة تحققه.
الحالة: ✅ منفذ ومبني في CI | 🔄 منفذ بانتظار تأكيد المستخدم على الجهاز | ⏳ ينتظر مدخلات

| # | المتطلب (بكلمات المستخدم) | التنفيذ | الحالة |
|---|---|---|---|
| 1 | طباعة لا تقول «فشل» ولا تعيد من البداية | `print/PrintEngine.kt` — حالات قابلة للاستئناف، حفظ التقدم على القرص، 3 محاولات لكل كرت حراري | ✅ |
| 2 | طباعة كميات كبيرة بأقل وقت، صفر أخطاء | PDF واحد متجهات مباشرة؛ `pipeline` نافذة 128 + جولتا إعادة صامتة | 🔄 |
| 3 | سرعة دخول ورفع وجلب عن بعد (دومين/VPN) | جلسة دائمة (`obtain/invalidateSession`)، `count-only` للعدادات والباقات، proplist مختصر | 🔄 |
| 4 | 5000 كرت رفعاً وطباعة خلال ~60 ثانية | نافذة 128 ≈ 400+ أمر/ثانية عن بعد + PDF موازٍ | 🔄 |
| 5 | PDF ملف واحد لا يتجزأ | `PdfExporter.exportPages` بلا تقسيم | ✅ |
| 6 | خطوة واحدة: توليد + رفع + طباعة (مثل سمارت) | `ui/screens/ExpressScreen.kt` | ✅ |
| 7 | مزامنة تلقائية بلا أي زر («اشتي كل شي تلقائي») | `data/SyncEngine.kt`: عدادات/متصلون كل 15ث، باقات وكروت تلقائياً، دمج يحفظ المحلي | 🔄 |
| 8 | الباقات تظهر وتُختار في كل مكان | جلب تلقائي + زر داخل حوار التوليد والسريع + شرائح في تعديل الكرت | 🔄 |
| 9 | تسجيل جيميل → تجربة أسبوع → قفل → طلب ترخيص | `license/LicenseManager.kt` (NeedsRegister/Trial/TrialEnded) | ✅ |
| 10 | ترخيص مقفول على الجهاز، لا يتجدد بإعادة التثبيت | بصمة ANDROID_ID + مفتاح ECDSA موقّع | ✅ |
| 11 | حساب واحد = جوال واحد فقط | فحص `boundDevice` في الأدمن قبل الإصدار | ✅ |
| 12 | ربط أدمن↔مشترك تلقائي + دردشة + مزامنة بيانات | `data/Backend.kt` + `ChatPanel.kt` — الكود كامل | ⏳ ينتظر بيانات Firebase من المستخدم |
| 13 | بديل عملي بلا سحابة: طلب/تفعيل عبر واتساب | `LicenseLink.parseRequestText/extractKey` + زر «لصق طلب من واتساب» | 🔄 |
| 14 | «المهندس علي واقص» + 776831921 في التطبيق | `SettingsScreen` (حول التطبيق) + `LicenseLink.WHATSAPP_NUMBER` | ✅ |
| 15 | التصدير التلقائي للراوتر عند التوليد والطباعة | توليد=رفع فوري؛ كل أزرار الطباعة ترفع المتبقي بالخلفية | 🔄 |
| 16 | توافق راوتر المستخدم RB1100 v6.49.20 | إصلاحات v6: `username`/`customer` الفعلي/`actual-profile`/`true` | 🔄 |
| 17 | تشخيص واضح بدل أصفار صامتة | تبويب «فحص شامل» في إدارة الراوتر — 13 أمراً خاماً بردودها | 🔄 |
| 18 | أقسام كاملة مثل Smart Creator V5 | رئيسية مقسمة + كروت/باقات/طباعة/تقارير/مبيعات/جلسات/إدارة راوتر/قوالب | 🔄 جولة تحسين مستمرة |

## أعطال أصلحت (اختبار رجوع يدوي عند كل تعديل ذي صلة)
- حرف L كان يُحذف من مفاتيح الترخيص → رفض ~97% من المفاتيح (License.kt normalize)
- الإصدار لجهاز وهمي عند لصق رابط الطلب كنص (extractDeviceCode + تحقق قبل الإصدار)
- رفع اليوزر منجر v6 بحقل name غير الموجود → فشل كامل (username=)
- جلب الباقات كان ينقل كل المستخدمين → تعليق على VPN (count-only لكل باقة)
- زر الرفع كان يكرر كل القائمة على الراوتر (فلتر uploaded + routerId)
